pipeline {
    agent {
        label 'linux-node1'
    }

    stages {
        stage('Example Stage') {
            steps {
                echo "Running on node: ${env.NODE_NAME}"
            }
        }
    }
}
