pipeline {
    agent {
        label 'linux-node1'
    }

    stages {
        stage('current logged in user') {
            steps {
                sh 'whoami'
            }
        }
    }

    post {
        success {
            echo 'Pipeline completed successfully!'
        }
    }
}


