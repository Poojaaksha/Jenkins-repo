pipeline {
    agent {
          label 'linux-node1'
     }

    environment {
        NAME = "Pooja"
    }

    stages {
        stage('Environment Variable') {
            steps {
                echo "${NAME}"
            }
        }
    }
}
