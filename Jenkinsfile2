pipeline {
    agent {
          label 'linux-node1'
     }

    parameters {
        string(name: 'NAME', defaultValue: 'Pooja')
    }

    stages {
        stage('Print Name') {
            steps {
                echo "${params.NAME}"
            }
        }
    }
}
