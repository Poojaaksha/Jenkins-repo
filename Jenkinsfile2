pipeline {
    agent {
          label 'linux-node1'
     }
    pipeline {
    agent any

    environment {
        NAME = "Pooja"
    }

    stages {
        stage('Environment Variable') {
            steps {
                echo "${NAME}"
            }
        }
    }
}
